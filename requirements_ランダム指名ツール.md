# ランダム指名ツール 要件定義書

作成日: 2026-04-27  
バージョン: 1.0

---

## 1. プロジェクト概要

### 1.1 目的・背景

授業や講義の場で教師がランダムに学生を指名するためのWebアプリ。
既存の学生情報データベース（PostgreSQL）を活用し、公平なランダム選択・履歴管理・柔軟なグループ管理を実現する。

### 1.2 ターゲットユーザー

- 主要ユーザー：教師（授業中にプロジェクターや手元PCで操作）
- 管理者：グループ設定などの初期セットアップを行う教師・担当者

### 1.3 対象プラットフォーム

- Webアプリ（ブラウザ動作）
- 学内Webサーバにデプロイ
- 対応ブラウザ：モダンブラウザ（Chrome / Edge / Safari 最新版）

### 1.4 スコープ

#### MVP（初回リリース）
- 学生DBからのデータ取得（class_group 単位）
- グループ作成・管理（アプリ内独自グループ）
- ランダム指名（全員消化後に自動リセット）
- 氏名 / 学籍番号 切替表示
- 指名履歴の表示
- 学生の除外機能（欠席者対応）

#### 将来拡張（Nice to have）
- 指名統計・可視化
- 複数セッション管理
- 教師アカウントによるパーソナライズ

---

## 2. 機能要件

### 2.1 機能一覧（MoSCoW 優先度付き）

| 機能名 | 概要 | 優先度 | 備考 |
|--------|------|--------|------|
| 学生データ取得 | バックエンドAPI経由でDBから学生一覧・class_group一覧を取得 | Must | APIエンドポイントは設定画面で変更可能 |
| グループ作成 | class_groupを組み合わせて任意のグループを作成・命名・保存 | Must | 複数class_group選択可能 |
| ランダム指名 | 選択中グループの未指名学生からランダムに1名を選出 | Must | 全員済みで自動リセット |
| 表示モード切替 | 指名結果を「氏名」または「学籍番号」で大きく表示 | Must | ヘッダーのトグルで切替 |
| 進捗バー | 指名済み人数 / 総人数をプログレスバーで表示 | Must | |
| 指名履歴 | 指名された学生の氏名・学籍番号・時刻を一覧表示 | Must | グループ単位で表示 |
| 除外機能 | 欠席者などを指名対象から除外・解除できる | Must | 学生管理タブで操作 |
| リセット | グループの指名状態をリセットして全員対象に戻す | Must | 確認ダイアログあり |
| 学生管理画面 | グループ内学生の一覧・指名済みバッジ・除外状態を確認・操作 | Should | |
| 設定画面 | APIエンドポイント・デフォルト表示モードの設定 | Should | |
| 学生データ再読み込み | 設定画面から手動でDBを再取得 | Should | |

### 2.2 機能詳細

#### ランダム指名ロジック
1. 選択中グループの学生から「除外」でも「指名済み」でもない学生を抽出
2. その中からランダムに1名を選択
3. 選択された学生を「指名済み」に追加
4. 残り0名になった時点で、次回指名ボタン押下時に自動リセット（全員から再選択）

#### グループ管理
- DBの `class_group` を1つ以上選択してグループを定義
- グループ名は自由入力
- グループ情報・指名状態・除外状態はブラウザの `localStorage` に保存（サーバー不要）
- グループは複数作成可能

#### 表示モード
- 「氏名」モード：氏名を大きく表示し、学籍番号をサブテキストで表示
- 「学籍番号」モード：学籍番号を大きく表示し、氏名をサブテキストで表示
- 選択はヘッダーのトグルボタンで即時切替

---

## 3. 画面一覧・画面遷移

### 3.1 画面一覧

| 画面名 | 概要 | 主な操作者 |
|--------|------|-----------|
| 指名タブ | メイン画面。グループ選択・指名ボタン・結果表示・履歴 | 教師 |
| 学生管理タブ | グループ内学生の一覧・除外操作 | 教師 |
| 設定タブ | APIエンドポイント設定・データ再読み込み | 管理者・教師 |
| グループ作成モーダル | グループ名入力・class_group選択 | 教師 |

### 3.2 画面遷移フロー

```
アプリ起動
  └─ 指名タブ（デフォルト）
       ├─ サイドバー：グループ選択 / 「グループを作成」ボタン
       │     └─ グループ作成モーダル → 保存 → 指名タブに戻る
       ├─ 指名ボタン → 結果表示 → 履歴に追記
       ├─ リセットボタン → 確認ダイアログ → リセット
       └─ タブ切替
             ├─ 学生管理タブ → 除外/解除操作
             └─ 設定タブ → API設定・再読み込み
```

### 3.3 各画面の主要コンポーネント

#### 指名タブ
- サイドバー：グループ一覧（指名済み数/総数バッジ付き）
- 指名カード：結果表示エリア（大きなフォント）
- プログレスバー：指名進捗
- 指名ボタン・リセットボタン
- 履歴リスト：直近の指名履歴

#### 学生管理タブ
- サイドバー：グループ選択
- 学生テーブル：学籍番号・氏名・クラス・指名済みバッジ・除外ボタン

#### グループ作成モーダル
- グループ名入力フィールド
- class_groupチェックボックス一覧（DB取得）

---

## 4. システム構成・アーキテクチャ

### 4.1 技術スタック

| レイヤー | 技術 | 備考 |
|--------|------|------|
| フロントエンド | HTML / CSS / Vanilla JS | シングルHTMLファイル、外部依存なし |
| バックエンド | Node.js + Express | RESTful API |
| DBドライバ | node-postgres（pg） | PostgreSQL接続 |
| データベース | PostgreSQL 16 | 既存学内DB（読み取り専用利用） |
| 状態永続化 | ブラウザ localStorage | グループ・指名状態・履歴 |
| ホスティング | 学内Webサーバ | オンプレミス |

### 4.2 システム構成概要

```
[ブラウザ] ──HTTP──→ [Node.js/Express] ──TCP:5432──→ [PostgreSQL 16]
  └── index.html         └── /api/students                └── students DB
  └── localStorage           /api/class-groups               └── students テーブル
```

- フロントエンドは `public/index.html` の単一ファイル
- バックエンドは `/api/students`、`/api/class-groups` の2エンドポイントのみ
- DBへの書き込みは行わない（読み取り専用）

### 4.3 APIエンドポイント仕様

| メソッド | パス | 概要 | クエリパラメータ |
|---------|------|------|----------------|
| GET | /api/students | 学生一覧取得 | `class_group`（省略時は全件） |
| GET | /api/class-groups | class_group一覧取得 | なし |

#### レスポンス例（GET /api/students）
```json
[
  {
    "id": "uuid",
    "student_number": "A00001",
    "name": "山田 太郎",
    "kana": "ヤマダ タロウ",
    "class_group": "Aクラス"
  }
]
```

### 4.4 外部連携・API

- 外部サービス連携なし
- 既存学内PostgreSQLへの読み取りアクセスのみ

---

## 5. 用語集・データ定義

### 5.1 用語集

| 用語 | 定義 |
|------|------|
| class_group | DBの `students.class_group` カラム。学生が所属するクラス・グループの区分 |
| グループ | アプリ内で定義する指名対象の集合。1つ以上の class_group を組み合わせて作成 |
| 指名済み | そのセッション内で既にランダム選択された学生 |
| 除外 | 欠席等の理由で指名対象から一時的に外された学生 |
| リセット | グループの「指名済み」状態をクリアし、全員を対象に戻す操作 |
| 自動リセット | 全員が指名済みになった後、次の指名ボタン押下時に自動で行われるリセット |
| 表示モード | 指名結果を「氏名」で大きく表示するか「学籍番号」で大きく表示するかの設定 |

### 5.2 主要エンティティ定義

#### 既存DB（読み取りのみ）

**students テーブル（参照カラム）**

| カラム | 型 | 用途 |
|--------|-----|------|
| id | UUID | 学生の一意識別子 |
| student_number | VARCHAR(6) | 学籍番号（表示用） |
| name | VARCHAR(255) | 氏名（表示用） |
| kana | VARCHAR(255) | 氏名カナ |
| class_group | VARCHAR(50) | クラス区分（グループ作成の基準） |

#### アプリ内データ（localStorage）

**グループ（sg_groups）**

| フィールド | 型 | 説明 |
|--------|-----|------|
| id | string | タイムスタンプベースの識別子 |
| name | string | グループ名（任意入力） |
| classGroups | string[] | 対象 class_group の配列 |

**指名済みセット（sg_picked）**

| フィールド | 型 | 説明 |
|--------|-----|------|
| [groupId] | string[] | 指名済み学生IDの配列（グループごと） |

**除外セット（sg_excluded）**

| フィールド | 型 | 説明 |
|--------|-----|------|
| [groupId] | string[] | 除外中学生IDの配列（グループごと） |

**履歴（sg_history）**

| フィールド | 型 | 説明 |
|--------|-----|------|
| id | string | 学生ID |
| name | string | 氏名 |
| number | string | 学籍番号 |
| group | string | グループ名 |
| time | string | 指名時刻（HH:MM） |

---

## 6. 未決事項・備考

| # | 事項 | 状況 |
|---|------|------|
| 1 | DBパスワードの管理方法 | 環境変数（`DB_PASSWORD`）での運用を推奨。設定手順を別途確認 |
| 2 | 複数端末での状態共有 | 現状 localStorage のためブラウザ単体で完結。将来的にサーバー側保存を検討 |
| 3 | 学生データの更新タイミング | 設定画面の「再読み込み」ボタンで手動更新。自動更新は将来拡張 |
| 4 | アクセス制限・認証 | 現バージョンは認証なし。学内ネットワーク限定での運用を前提とする |
| 5 | ポート番号 | デフォルト 3000。学内サーバの空きポートに合わせて `PORT` 環境変数で変更可能 |

---

## 7. 技術スタック別ベストプラクティス・参考リソース

### 7.1 フロントエンド

| リソース名 | URL | 概要 |
|-----------|-----|------|
| MDN Web Docs | https://developer.mozilla.org/ja/ | HTML/CSS/JS 公式リファレンス |
| Web Storage API | https://developer.mozilla.org/ja/docs/Web/API/Web_Storage_API | localStorage の仕様・セキュリティ注意事項 |

### 7.2 バックエンド

| リソース名 | URL | 概要 |
|-----------|-----|------|
| Express.js 公式 | https://expressjs.com/ | Node.js Webフレームワーク |
| node-postgres (pg) | https://node-postgres.com/ | Node.js 用 PostgreSQL クライアント |
| Express セキュリティベストプラクティス | https://expressjs.com/en/advanced/best-practice-security.html | 本番運用時のセキュリティ設定 |

### 7.3 インフラ

- 学内オンプレミスWebサーバ（Node.js の実行環境が必要）
- Node.js 18 LTS 以上を推奨
- `pm2` などのプロセスマネージャで常駐化を推奨

---

## 8. セキュリティ評価・専門家コメント

### 8.1 脅威モデル（主なリスク）

| リスク | 深刻度 | 説明 |
|--------|--------|------|
| DBパスワードの漏洩 | High | `server.js` にパスワードをハードコードした場合、ソースコード流出時に即座に漏洩する |
| 不正アクセス（内部） | Medium | 認証なしのため、学内ネットワーク内の全員がアクセス可能 |
| SQLインジェクション | Low | `class_group` パラメータをクエリに使用しているため対策が必要 |
| XSS | Low | 学生名などのDB値をそのままDOMに描画するため、DBデータに悪意ある文字列が含まれる場合にリスクあり |
| localStorageデータ改ざん | Low | 指名履歴・グループ情報はクライアント側に保存されるため改ざん可能 |

### 8.2 必須セキュリティ対策

**DBパスワード管理（High 対応）**
- パスワードは必ず環境変数（`DB_PASSWORD`）で管理し、ソースコードに記述しない
- `.env` ファイルを使用する場合は `.gitignore` に追加する

**SQLインジェクション対策**
- `pg` のプリペアドステートメント（`$1` プレースホルダ）を使用する（実装済み）
- ユーザー入力は直接クエリに埋め込まない

**XSS対策**
- JS でDOMに値を描画する際は `textContent` を使用する（`innerHTML` 不使用）
- 実装済みであることを確認すること

**ネットワーク制限**
- 学内ネットワーク（イントラネット）からのみアクセス可能な状態で運用する
- 外部公開が必要な場合は認証機能を追加すること

### 8.3 推奨セキュリティ対策

- `helmet`（Expressミドルウェア）を導入してHTTPヘッダーを強化する
- アクセスログをサーバー側に記録し、不審なアクセスを検知できるようにする
- DBユーザー `subamanage` の権限を `students` テーブルの SELECT のみに制限する
- Node.js および npm パッケージを定期的にアップデートする（`npm audit` の活用）

### 8.4 準拠すべき規格・法令

- **個人情報保護法**：学生の氏名・学籍番号は個人情報に該当。学内ネットワーク限定での運用とし、外部への持ち出しを制限する
- **OWASP Top 10**：A03（インジェクション）、A07（認証の失敗）への対応を優先する

### 8.5 セキュリティ専門家からの総合コメント

本アプリは学内限定の運用を前提としており、外部からの攻撃リスクは低い。
ただし **DBパスワードの環境変数管理** は必須対応事項であり、設計段階で確実に組み込むこと。

また、DBユーザー権限の最小化（SELECT のみ）も重要で、万一アプリが侵害された場合のDB書き換えリスクを排除できる。
外部公開が将来的に必要になった場合は、認証機能の追加が前提条件となる。
